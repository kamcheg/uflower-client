{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "db": "json-server --watch db.json --port 4000 --delay 2000",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.10.1",
    "element-plus": "^2.9.6",
    "json-server": "0.17.4",
    "nuxt": "^3.16.0",
    "pinia": "^3.0.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "sass": "^1.85.1"
  }
}
